services:
  smashmap_postgres:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: smashmap
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: